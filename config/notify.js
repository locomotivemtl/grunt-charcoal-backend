/**
 * @file Send notifications to the CLI and by extension the OS
 */

module.exports = {
    php: {
        options: {
            message: 'PHP linted and cleaned up'
        }
    },
    watch: {
        options: {
            message: 'Keeping an eye out, Chief!'
        }
    }
};
